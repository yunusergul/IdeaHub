{
  "name": "ideahub",
  "private": true,
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:migrate": "cd apps/server && npx prisma migrate dev",
    "db:seed": "cd apps/server && npx prisma db seed",
    "db:reset": "cd apps/server && npx prisma migrate reset --force"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
